{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal local main window permissions",
  "windows": ["main"],
  "permissions": ["core:default"]
}
